package com.github.njuro.jard.attachment;

public enum UserContentStorageMode {
  LOCAL, // user files are stored in local filesystem
  AWS // user files are stored both locally and on remote AWS S3 server
}
